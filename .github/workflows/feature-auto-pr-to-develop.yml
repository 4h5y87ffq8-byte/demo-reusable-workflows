name: 🚀 Auto PR to Develop

on:
  workflow_call:
    secrets:
      TOKEN:
        description: "GitHub token for creating pull requests"
        required: true

jobs:
  create-pr-to-develop:
    name: 🔀 Create PR to Develop
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout code
        uses: actions/checkout@v4

      - name: 🔀 Create Pull Request to Develop
        env:
          GH_TOKEN: ${{ secrets.TOKEN }}
        run: |
          # Verificar se já existe um PR aberto da branch atual para develop
          existing_pr=$(gh pr list --head ${{ github.ref_name }} --base develop --json number --jq '.[0].number' || echo "")

          if [ -n "$existing_pr" ]; then
            echo "✅ PR já existe: #$existing_pr - Atualizando..."
            
            # Atualizar título e body do PR existente
            gh pr edit $existing_pr \
              --title "Auto PR: ${{ github.ref_name }} → develop (Build #${{ github.run_number }})" \
              --body "PR automático criado pelo workflow após build bem-sucedido da branch \`${{ github.ref_name }}\`.

            **🔄 Última atualização:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')
            **📦 Build:** #${{ github.run_number }}
            **🔗 Workflow:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            **📝 Commit:** \`${{ github.sha }}\`"
            
            echo "✅ PR #$existing_pr atualizado com sucesso!"
          else
            # Criar novo PR
            gh pr create \
              --base develop \
              --head ${{ github.ref_name }} \
              --title "Auto PR: ${{ github.ref_name }} → develop" \
              --body "PR automático criado pelo workflow após build bem-sucedido da branch \`${{ github.ref_name }}\`.

            **🔄 Última atualização:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')
            **📦 Build:** #${{ github.run_number }}
            **🔗 Workflow:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            **📝 Commit:** \`${{ github.sha }}\`"
              --assignee @me
            echo "✅ PR criado com sucesso!"
          fi
