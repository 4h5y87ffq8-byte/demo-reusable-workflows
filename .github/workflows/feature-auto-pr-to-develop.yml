name: 🚀 Auto PR to Develop

on:
  workflow_call:

jobs:
  create-pr-to-develop:
    name: 🔀 Create PR to Develop
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout code
        uses: actions/checkout@v4

      - name: Print secret (should be empty for security)
        run: echo "${{ secrets.TOKEN }}"

      - name: 🔀 Create Pull Request to develop
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.TOKEN }}
          base: develop
          branch: ${{ github.ref_name }}
          title: 'Auto PR: ${{ github.ref_name }} → develop'
          body: |
            PR automático criado pelo workflow após build bem-sucedido da branch `${{ github.ref_name }}`.
          draft: false
          push-to-fork: false
          commit-message: '[create-pull-request] automated change'
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          author: mandico <43236302+mandico@users.noreply.github.com>
          signoff: false
          delete-branch: false

